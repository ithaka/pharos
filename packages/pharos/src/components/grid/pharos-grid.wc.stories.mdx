import { Story, Canvas, Meta, ArgsTable } from '@storybook/addon-docs/blocks';
import { html } from 'lit-html';
import { viewports } from '../../pages/shared/viewports';

import './pharos-grid';
import '../sidenav/pharos-sidenav';
import '../link/pharos-link';

<Meta
  title="Components/Grid"
  parameters={{
    layout: 'fullscreen',
    viewport: {
      viewports,
    },
  }}
/>

export const gridItems = (rows) => {
  const arr = [];
  rows.forEach((items) => {
    for (let i = 0; i < items; i++) {
      arr.push({ span: 12 / items, cols: items });
    }
  });
  return arr.map(
    (item) => html`<div class="grid-example__item" style="grid-column: span ${item.span}">
      ${item.cols}
    </div>`
  );
};

export const comboItems = (rows) => {
  const arr = [];
  rows.forEach((item) => {
    arr.push({ span: item, cols: item });
    arr.push({ span: 12 - item, cols: 12 - item });
  });
  return arr.map(
    (item) => html`<div class="grid-example__item" style="grid-column: span ${item.span}">
      Span ${item.cols}
    </div>`
  );
};

export const gridColumns = () => {
  const arr = [];
  for (let i = 1; i <= 12; i++) {
    arr.push(html`<div class="grid-example__column">${i}</div>`);
  }
  return arr;
};

# Grid

<Canvas>
  <Story name="One column">{html` <pharos-grid>${gridColumns()}</pharos-grid> `}</Story>
</Canvas>

## API

<ArgsTable of="pharos-grid" />

# One column with sidenav

<Canvas>
  <Story name="One column with sidenav" parameters={{ docs: { disable: true } }}>
    {html`
      <div
        style="display: grid; grid-template-areas: 'sidenav main'; grid-template-columns: max-content 1fr"
      >
        <pharos-sidenav style="grid-area: sidenav">
          <pharos-link slot="top" href="/" id="jstor-logo">
            <img src="./images/jstor-logo-inverse.svg" alt="Pharos Home" width="72" height="100" />
          </pharos-link>
        </pharos-sidenav>
        <main style="grid-area: main">
          <pharos-grid layout="1-col--sidenav">${gridColumns()}</pharos-grid>
        </main>
      </div>
    `}
  </Story>
</Canvas>

# Two column

<Canvas>
  <Story name="Two column">
    {html`
      <pharos-grid layout="2-col">
        <div class="grid-example__container--first"></div>
        <div class="grid-example__container--second"></div>
        <div class="grid-example__container--third"></div>
      </pharos-grid>
      <pharos-grid layout="2-col" style="position: absolute; top: 0; width: 100%"
        >${gridColumns()}</pharos-grid
      >
    `}
  </Story>
</Canvas>

# Column layouts

<Canvas>
  <Story name="Column layouts">
    {html`
      <pharos-grid style="margin: 1rem 0">
        ${gridItems([1, 2, 3, 4, 6])} ${comboItems([7, 8, 9, 10])}
      </pharos-grid>
    `}
  </Story>
</Canvas>
