---
layout: '@layouts/MarkdownLayout.astro'
title: 'Loading Spinner'
description: 'Loading spinners indicate that a page, section, or content is loading.'
---

import { mdxComponents } from '@components/markdown/mdxComponents';
import Example from '@components/markdown/Example.astro';
import BestPractices from '@components/BestPractices.astro';
import CodeBlock from '@components/CodeBlock.astro';

export const components = mdxComponents;

export const loadingSpinnerCode = `
            <div
              id="loading-spinner"
              className="loading-spinner js-loading-spinner"
              role="alert"
              aria-live="assertive"
            >
              <p className="vh js-loading-spinner-copy">Content is loading...</p>
            </div>
          `;

<Example storyBookType="components" componentTitle="Loading Spinner">
  <div style="height: 5rem; width: 5rem; border: 1px solid black; position: relative;">
    <site-pharos-loading-spinner></site-pharos-loading-spinner>
  </div>
</Example>

## Usage

Loading spinners should be placed over pages or their sections that need to load before the
user can continue with their experience.

## Best practices

<BestPractices>
  <Fragment slot="do">
    Use loading spinners to indicate progress when the time remaining can't be determined
  </Fragment>
  <Fragment slot="dont">
    Don't use loading spinners to indicate an action the user took is being processed or when
    the time remaining is known. Use the progress bar instead.
  </Fragment>
</BestPractices>

## Accessibility

### Relevant WCAG guidelines

- [4.1.3 Status Messages](https://www.w3.org/WAI/WCAG21/Understanding/status-messages.html)

### Importance

Loading spinners visually (and audibly) inform the user that something is in the process of
loading

### Code expectations

- Role = "alert"
- Add an aria-live = "assertive" to the loading `<div>` element
- Add a visually hidden `<p>` tag that has a message for assistive technology
  within that div
  - Ex. "content is loading"
- Add visually hidden text, triggered by Javascript to announce when content has loaded
  - Ex. "content has loaded"
- [Example to reference](https://codepen.io/stuartjnelson/pen/xWyLGR): (Side note that styling in example can be changed. The example is provided to demonstrate
  the screen reader interaction)

<CodeBlock language="html" code={loadingSpinnerCode} />

### Screen reader

- When spinner is active - audible message: "Content is loading"
- When content is done loading - audible message: "Content is loaded"

### Keyboard

- No specific keyboard commands since aria-live announces "assertive" automatically
